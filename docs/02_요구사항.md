# WHENCALENDAR — 요구사항

> 프로젝트 정의는 [01_프로젝트.md](01_프로젝트.md), 기술 결정은 [03_기술_스펙.md](03_기술_스펙.md). ID 체계와 문서 형식은 WHENNOTE `docs/02_요구사항.md`를 따른다. 2026-09-17 정의. 체크는 자동 테스트가 증명한 것만 — UI 항목은 실사용으로 확인한 뒤 체크한다.

**Core Value:** 몰입해도 시간을 잃지 않는다. 창을 열지 않아도, 보려고 하지 않아도, 다음 하나까지 얼마 남았는지 안다.

## v1 Requirements

### 오버레이 (OVL)

이 앱의 존재 이유다. 나머지 그룹은 전부 이걸 받쳐 준다.

- [ ] **OVL-01**: 화면 상단 가운데에 아일랜드가 상주한다. 프레임 없는 투명 창이고, 앱을 실행한 뒤 아무 설정 없이 바로 뜬다
- [x] **OVL-02**: 오버레이는 **전체화면 앱 위에** 뜬다. 전체화면 IDE·브라우저 영상·발표 화면에서 가려지지 않는다 — 2026-09-17 실측. `screen-saver` 레벨만으로는 가려졌고 1초 주기 `moveTop()`으로 해결([D-14](03_기술_스펙.md#d-14-z-order는-주기적으로-다시-잡는다)). 전체화면 앱이 뜬 직후 최대 1초는 가려지고, exclusive fullscreen은 범위 밖
- [ ] **OVL-03**: 오버레이는 **마우스 클릭을 통과시킨다.** 아일랜드 아래에 있는 창을 그대로 누를 수 있다
- [ ] **OVL-04**: 오버레이는 **포커스를 뺏지 않는다.** 뜨거나 펼쳐질 때 타이핑하던 창의 커서가 유지된다
- [x] **OVL-05**: 남은 시간에 따라 다섯 단계로 변한다 — 60분 초과: 링만(접힘) · 30분부터: 이름과 남은 시간이 펼쳐지기 시작 · 10분: 완전히 펼침 · 5분: 색이 붉어지고 번짐 · 1분: 테두리가 맥동. 어느 단계에서도 클릭을 요구하거나 화면을 가리지 않는다
- [ ] **OVL-06**: 링 게이지가 60분을 한 바퀴로 남은 양을 보여 준다. 60분 이상 남으면 꽉 찬 채로 멈춰 있다
- [ ] **OVL-07**: 오늘 남은 일정 개수가 점으로 보인다. 다음 하나만 색이 들어오고 나머지는 회색이다
- [x] **OVL-08**: 일정이 **진행 중**이면 색이 중립(청록)으로 바뀌고 "진행 중" 배지가 붙는다. 링은 끝날 때까지를 센다
- [x] **OVL-09**: 진행 중인 일정이 **끝나기 5분 전**이면 색이 다시 들어오고 "곧 종료"로 바뀐다. 설정에서 끌 수 있다
- [ ] **OVL-10**: 오늘 남은 일정이 없으면 오버레이가 조용히 숨는다. 마우스를 올리면 다시 보인다
- [ ] **OVL-11**: 아일랜드에 **마우스를 올리면** 시간과 무관하게 펼쳐지고, 자동 펼침에는 없는 **그 다음 일정**까지 따라 나온다. 마우스를 빼면 원래 상태로 돌아간다
- [x] **OVL-12**: 설정에서 펼침 시작 시각, 완전 펼침 시각, 링 크기(16·20·26px), 끝나기 5분 전 알림, OS 알림과 알림 시점을 바꾼다. 값을 바꾸면 설정 화면의 미리보기가 즉시 같이 바뀐다. **위치와 대상 모니터는 아직 없다** — 오버레이는 주 모니터 상단 가운데에 고정이고, 그 선택지는 PLAT-04로 미룬다
- [x] **OVL-13**: 본체 창에서 <kbd>O</kbd>로 오버레이를 잠시 끈다. 발표·녹화 중에 쓴다
- [ ] **OVL-14**: 시스템 잠금·절전에서 돌아왔을 때 오버레이가 현재 시각 기준으로 즉시 올바른 상태를 보여 준다

### 일정 소스·구독 (SUB)

- [x] **SUB-01**: `.ics` URL을 등록하면 일정을 읽어 온다. **읽기 전용이고 OAuth·로그인이 필요 없다.** 구글 캘린더의 비공개 iCal 주소를 붙여 쓴다
- [x] **SUB-02**: 구독은 정해진 주기로 자동 갱신되고, <kbd>R</kbd>로 즉시 갱신한다. 마지막 갱신 시각이 보인다
- [x] **SUB-03**: **구독 갱신이 실패해도 마지막에 받아온 일정은 지우지 않는다.** 네트워크가 끊겼다고 오늘 일정이 사라지면 안 된다
- [x] **SUB-04**: 갱신 실패는 원인과 **다음에 할 행동**까지 화면에 적는다. "동기화 실패"로 끝내지 않는다
- [x] **SUB-05**: 구독마다 색이 하나씩 붙는다. 추가할 때 자동 배정되고 <kbd>C</kbd>로 바꾼다. 색은 6종이며 배정 순서는 상태색(성공·경고·실패)과 같은 값을 뒤로 미룬다
- [x] **SUB-06**: 구독을 <kbd>Space</kbd>로 켜고 끈다. 끄면 일정이 화면에서 빠지되 구독 자체는 남는다
- [x] **SUB-07**: `.ics`의 반복 규칙(RRULE)과 예외(EXDATE·RECURRENCE-ID)를 해석해 실제 날짜로 펼친다. 자체 반복 규칙을 만들지 않는다
- [x] **SUB-08**: `.ics`의 타임존(TZID)을 해석한다. 해외 일정이 시각이 어긋난 채 들어오지 않는다

### 본체 창 (WIN)

- [x] **WIN-01**: 전역 단축키로 본체 창을 열고 닫는다. 창 위치·크기를 기억한다 (WHENWORK `place.mjs` 승계)
- [x] **WIN-02**: 탭은 오늘 · 주 · 월 · 구독 · 설정이고 <kbd>Tab</kbd>으로 돈다. 탭에 건수 배지가 붙는다
- [x] **WIN-03**: **오늘** 탭은 시간축 목록이다. 지나간 일정은 흐려지고, 지금 시각에 선이 그어지고, 진행 중인 것과 다음 하나가 구분된다
- [x] **WIN-04**: **주** 탭은 요일별 밀도 띠 + 날짜별 세로 목록이다. 격자를 쓰지 않는다 — 키보드로 훑는 데는 목록이 빠르다
- [x] **WIN-05**: **월** 탭은 격자다. 칸마다 칸 높이가 허락하는 만큼(최대 3건) + `+N`으로 적고, 여러 날에 걸친 일정은 칸 위를 지나는 얇은 선으로 잇는다(이름은 시작한 칸에). 고른 날의 전체 목록은 아래 한 줄에 펼친다. <kbd>hjkl</kbd>과 방향키로 날짜를 옮기고, 달은 <kbd>[</kbd> <kbd>]</kbd>(·<kbd>PageUp</kbd>/<kbd>PageDown</kbd>·<kbd>Shift</kbd>+←→)로 넘긴다
- [x] **WIN-06**: 여러 날에 걸친 일정은 월 격자에서 **칸 위를 가로지른다.** 주 경계를 넘으면 두 조각으로 나뉘고 잘린 쪽 모서리로 이어짐을 표시한다. 레인은 두 줄까지 쌓고 그 뒤는 `+N`으로 접는다
- [x] **WIN-07**: 마우스 없이 전부 조작된다. <kbd>?</kbd>로 전체 키맵을 본다. 키 글자의 뜻은 WHENWORK와 같다(<kbd>E</kbd> 제목, <kbd>X</kbd> 삭제, <kbd>U</kbd> 되돌리기, <kbd>/</kbd> 검색)
- [x] **WIN-08**: 창을 닫아도 앱은 트레이에 남는다. 오버레이는 계속 동작한다
- [x] **WIN-09**: 기본 창 크기는 960×640, 최소 760×540이다

### 일정 등록·편집 (EV)

- [x] **EV-01**: <kbd>N</kbd>으로 한 줄 입력을 받아 일정을 만든다. `내일 15:00 치과 1시간` 같은 표현을 **로컬 규칙 파서**가 해석한다. 네트워크·API 키가 필요 없다. 여러 날에 걸친 종일 일정도 한 줄로 만든다 — `9/21~9/25 워크숍` · `21일부터 25일까지 워크숍` · `내일부터 3일간 출장` (시각을 말한 줄은 기간으로 보지 않는다)
- [x] **EV-02**: 해석 결과를 **등록 전에 보여 주고** <kbd>Enter</kbd>를 받는다. 자동으로 넣지 않는다. 애매한 부분("3시를 오후로 봤습니다")은 눈에 띄게 표시하고 그 자리에서 뒤집을 수 있다
- [x] **EV-03**: 해석에 실패하면 거절하지 않고 구조화 입력(날짜·시각·제목)으로 넘어간다. 입력한 글자는 제목으로 남는다
- [x] **EV-04**: <kbd>Enter</kbd>로 상세를 연다. 상세는 목록을 밀어내지 않고 오른쪽에 붙는다
- [x] **EV-05**: 제목·날짜·시각·반복·알림·달력을 각각 한 글자 키로 고친다. **구독으로 들어온 일정은 읽기 전용**이고 그 사실이 화면에 드러난다
- [x] **EV-06**: 삭제는 소프트 삭제이고 <kbd>U</kbd>로 되돌린다. 30일이 지난 것만 실제로 지워진다 (WHENWORK 삭제 규칙 승계)
- [x] **EV-07**: 직접 등록한 반복 일정을 고칠 때 "이 하나만 / 이후 전부"를 묻는다. 예외는 RFC 5545 방식으로 저장한다
- [x] **EV-08**: 일정마다 알림 시각(N분 전)을 둘 수 있다. 알림은 OS 알림이 아니라 오버레이 단계 변화로 표현하는 것이 기본이고, OS 알림은 설정에서 켠다

### 시간 찾기 (FIND)

- [x] **FIND-01**: <kbd>G</kbd>로 빈 시간을 찾는다. 길이(30분·1시간·2시간·반나절)와 시간대(업무 시간·아무 때나)를 고르면 후보가 나온다
- [x] **FIND-02**: 각 후보에 앞뒤 일정이 무엇인지와 그 사이 어디가 비는지가 함께 보인다
- [x] **FIND-03**: <kbd>Enter</kbd>로 그 자리에 바로 일정을 만든다
- [x] **FIND-04**: <kbd>Y</kbd>로 고른 후보들을 **클립보드에 복사**한다. 메일·메신저로 보내지 않는다 — 붙여넣는 것은 사람이 한다 (WHENMAIL "초안까지만" 승계)
- [x] **FIND-05**: 복사되는 내용에 **일정 제목이 들어가지 않는다.** 빈 시간만 적는다. 상대에게 내 일정 내용이 새어 나가면 안 된다
- [x] **FIND-06**: 복사 형식(목록·문장·표)과 말투(평어·높임)를 고른다

### 검색 (SRCH)

- [x] **SRCH-01**: <kbd>/</kbd>로 검색한다. 부분 문자열과 한글 초성으로 찾는다 (WHENNOTE SRCH-01·02 승계)
- [x] **SRCH-02**: **지난 일정도 찾는다.** 결과는 앞으로 올 것과 지난 것을 나눠 보여 준다 — 실제 쓰임은 "그 회의 언제였지"다
- [x] **SRCH-03**: 일치한 글자가 강조된다

### 저장소 (STOR)

- [x] **STOR-01**: 앱은 외부 DB·컨테이너 없이 앱 데이터 폴더 안의 내장 저장소(단일 파일)에 일정·구독·설정을 저장한다 (WHENNOTE STOR-01 승계)
- [x] **STOR-02**: 저장소 스키마에 버전이 있고 업데이트 시 사용자 개입 없이 자동 마이그레이션된다. 이행 직전 자동 백업이 남는다 (WHENNOTE STOR-02 승계)
- [x] **STOR-03**: 자신보다 높은 스키마 버전의 DB를 만나면 쓰지 않고 안내한다. 손상된 DB는 지우지 않고 옆에 보관한다 (WHENWORK D-13·D-15 승계)
- [x] **STOR-04**: 구독으로 받아온 일정과 직접 등록한 일정이 저장소에서 구분된다. 구독을 지우면 그 일정만 빠지고 직접 등록한 것은 남는다

### 내보내기·가져오기 (DATA)

- [x] **DATA-01**: 설정에서 전체 데이터를 JSON 파일 하나로 내보낸다 (WHENNOTE DATA-01 승계)
- [x] **DATA-02**: 내보낸 JSON을 다른 PC에서 가져오면 같은 상태로 복원된다. 왕복 테스트가 있다. 가져오기 직전 자동 백업된다
- [x] **DATA-03**: 직접 등록한 일정을 `.ics` 파일로 내보낸다. 표준 형식이라 다른 캘린더가 읽는다

### 첫 실행·플랫폼 (PLAT)

- [ ] **PLAT-01**: 설치 후 첫 실행에 설정 화면 없이 오버레이가 뜬다. 구독이 없으면 무엇을 하면 되는지 한 화면으로 안내한다
- [x] **PLAT-02**: 전역 단축키 등록이 실패하면(형제 앱과 충돌 포함) 알리고 설정에서 다시 지정한다. 바꾼 조합의 등록 성공 여부를 확인한다 (WHENNOTE PLAT-02 승계)
- [x] **PLAT-03**: 설정에 로그인 시 자동 실행 토글이 있다
- [ ] **PLAT-04**: 멀티 모니터에서 주 모니터만 쓸지 전부 쓸지 고른다. 모니터 구성이 바뀌면 오버레이가 따라간다 — **뒤쪽 절반만 되어 있다.** 오버레이는 주 모니터에 고정으로 붙고, `display-added`·`display-removed`·`display-metrics-changed`를 듣고 주 모니터 기준으로 다시 배치한다(`main/overlay.mjs`의 `relocate`). 어느 모니터에 띄울지 **고르는 수단은 없다**
- [ ] **PLAT-05**: 형제 앱 세 개와 같은 PC에서 동시에 실행돼도 트레이·단축키·데이터 폴더가 서로 간섭하지 않는다
- [x] **PLAT-06**: OS 분기는 `main/platform/` 안에만 있다. 계약 테스트가 이를 지킨다 (WHENWORK 승계) — `test/platform.test.mjs`가 `main/` 전체를 훑어 `process.platform`·OS 이름이 그 폴더 밖에 나오면 실패한다. `process.platform`을 읽는 줄은 `main/platform/index.mjs` 하나뿐이다
- [x] **PLAT-07**: macOS에서 메뉴 막대에 상주한다 — Dock·<kbd>Cmd</kbd>+<kbd>Tab</kbd>에 뜨지 않고(LSUIElement), 메뉴 막대 아이콘은 Template이라 다크·라이트를 따라가며, 오버레이는 메뉴 막대 아래(`workArea`)에 붙어 노치를 피한다. Dock에서 빠진 앱이 잃는 <kbd>Cmd</kbd>+<kbd>C</kbd>·<kbd>V</kbd>는 응용 프로그램 메뉴를 두어 되살린다

### 형제 앱 연동 (SIB)

WHENCOMMAND(시리즈의 입력줄)가 이 앱의 명령을 부를 수 있게 한다. 규약은 [when-protocol](https://github.com/when630/when-protocol), 앞선 구현은 WHENNOTE 0.1.1·WHENWORK 0.2.1. **이 그룹이 없어도 이 앱은 온전하다.**

- [ ] **SIB-01**: `whencalendar://` 스킴을 OS에 등록하고 `add`·`search`·`open` 세 명령을 받는다. 떠 있으면 기존 인스턴스가 받는다(Windows `second-instance` argv, macOS `open-url`). 모르는 명령·스킴은 조용히 무시한다 ([D-33](03_기술_스펙.md#d-33-형제-앱-연동은-딥링크와-매니페스트-파일이다-2026-09-21))
- [ ] **SIB-02**: 실행될 때마다 `~/.when/apps/whencalendar.json`에 명령 목록을 덮어쓴다. 쓰기에 실패해도 앱은 멈추지 않는다
- [ ] **SIB-03**: `add?text=`는 한 줄을 새 일정 대화상자에 채우고 읽은 결과를 보인다 — **확인 없이 넣지 않는다**(Enter로 확정). `search?q=`는 검색을 열어 검색어를 넣고, `open`은 창을 연다. 창이 아직 로딩 중이어도 잃지 않는다

### 릴리스 (REL)

- [x] **REL-01**: Windows NSIS 설치 파일이 electron-builder로 빌드된다
- [x] **REL-02**: GitHub Actions가 태그 푸시 시 빌드해 Releases 드래프트에 올린다. 사람이 공개한다
- [x] **REL-03**: 앱이 새 버전을 감지해 알리고 Windows는 종료 시 설치한다 (WHENWORK `update.mjs` 승계)
- [x] **REL-04**: README에 설치·미서명 첫 실행 우회·단축키·데이터 위치가 있다
- [x] **REL-06**: macOS 설치 파일(dmg·zip, arm64·x64)이 같은 태그에서 함께 빌드된다. 인증서가 없으므로 ad-hoc 서명만 붙인다(`tools/adhoc-sign.cjs`) — 서명이 아예 없으면 Apple 실리콘에서 실행 자체가 막힌다. Gatekeeper 경고 우회는 README에 있다
- [ ] **REL-05**: 패키징된 설치본에서 스모크 테스트가 통과한다. **오버레이가 전체화면 앱 위에 뜨는지를 설치본에서 확인한다**(OVL-02, `poc/verify-fullscreen.ps1`) — 2026-09-18 설치본 스모크 통과(`store·tray·version` 확인). 전체화면 위 표시는 **설치본에서 아직 미검증**(개발 실행에서는 확인됨)

### 완료 판정 (DONE)

- [ ] **DONE-01**: 작성자가 2주 사용한 뒤 "몰입하다 일정을 놓치거나 늦은 날"이 0일이다

## v2 Requirements

- **NL-01**: 규칙 파서가 못 알아듣는 문장을 LLM으로 넘긴다. 설정에 키를 넣었을 때만 켜지고, 기본값은 규칙 파서 단독 ([03 §자연어](03_기술_스펙.md))
- **NL-02**: 클립보드·스크린샷에서 일정 제안 — 카톡·메일에서 복사한 문장을 읽어 일정 후보를 만든다
- **NL-03**: 음성 한 줄 입력 (whisper.cpp 로컬)
- ~~**MAC-01**: macOS 지원~~ → **2026-09-19 v1로 들어옴**(PLAT-06·07, REL-06). 남은 것은 **전체화면 위 표시의 실기기 검증** 하나다(REL-05) — `screen-saver` 레벨로 서 있지만 macOS에서 눈으로 본 적이 없다
- **CAL-01**: 구독 달력에 쓰기 (OAuth가 필요해 보류)
- **TZ-01**: 타임존 전환 UI — 해외 미팅 시각을 현지·내 시각 양쪽으로
- **TRUST-01**: 코드 서명·공증

## Out of Scope

[01_프로젝트.md §제외](01_프로젝트.md#제외) 참고. 이 문서에서는 반복하지 않는다.

## Traceability

| Requirement | Phase | Status |
|---|---|---|
| OVL-01 ~ OVL-05, OVL-11 | Phase 0 (PoC) | Prototyped 2026-09-17 — `poc/`에서 동작 확인. **OVL-02는 스크린샷으로 검증 완료**(`poc/verify-fullscreen.ps1`), OVL-03·OVL-11은 실사용 검증 전 |
| OVL-01 ~ OVL-14, STOR-01 ~ STOR-03 | Phase 1 (트레이서) | Implemented 2026-09-17 — 테스트 31개·스모크 통과. 상태 계산(OVL-05·08·09)과 저장소(STOR-01~03)는 테스트가 증명한다. 화면으로만 확인되는 것(OVL-01·03·04·06·07·10·11·13·14)은 **실사용 확인 전** |
| SUB-01 ~ SUB-08, STOR-03 ~ STOR-04 | Phase 2 | Implemented 2026-09-17 — 테스트 36개(ics 22 · sync 14). 공개 `.ics`(구글 한국 공휴일)로 실제 구독까지 확인 |
| WIN-01 ~ WIN-09, EV-01 ~ EV-03, EV-06 | Phase 3 (일부) | Implemented 2026-09-17 — 오늘·주·월·구독 탭, 한 줄 입력, 삭제·되돌리기 |
| EV-04 ~ EV-05 | Phase 3 | Implemented 2026-09-17 — 상세 패널, 제목·시간 편집. 구독 일정은 읽기 전용 |
| EV-07 ~ EV-08 | Phase 3 | Implemented 2026-09-18 — 회차 단위 편집·삭제(테스트 10개), 알림(테스트 8개) |
| FIND-01 ~ FIND-06, SRCH-01 ~ SRCH-03 | Phase 4 (일부) | Implemented 2026-09-17 — 테스트 31개(free 18 · search 11 · parity 2) |
| DATA-01 ~ DATA-03 | Phase 4 | Implemented 2026-09-18 — JSON 왕복·.ics 내보내기. 테스트 12개 |
| OVL-12 · PLAT-03 (설정 화면) | Phase 5 (일부) | Implemented 2026-09-18 — 값을 바꾸면 미리보기가 같이 바뀐다 |
| REL-01 ~ REL-04, PLAT-02 | Phase 5 | Implemented 2026-09-18 — 설치 파일·자동 업데이트·CI·README |
| PLAT-06·07, REL-06 (macOS) | Phase 5 | Implemented 2026-09-19 — OS 분기를 `main/platform/`으로 모으고(계약 테스트 9개) dmg·zip 빌드를 더했다. 패키징한 앱의 스모크는 Apple 실리콘에서 통과 |
| REL-05, PLAT-01·04·05 | Phase 5 (남음) | 설치본 실사용 검증 — **전체화면 위 표시는 macOS에서 미검증** |
| DONE-01 | Phase 6 | Pending |

- v1 requirements: 64 (2026-09-19 PLAT-07·REL-06 추가 — macOS)
- Mapped: 64 · Unmapped: 0
- 체크 완료: 56 (자동 테스트·스크린샷이 증명한 것만)

**Phase 1이 트레이서인 이유** — 오버레이가 이 앱의 전부다. 구독도 본체 창도 없이 **하드코딩한 일정 배열 하나로 오버레이만 먼저 완성**해서, 몰입 중에 실제로 쓸 만한지부터 확인한다. 여기서 아니면 나머지를 만들 이유가 없다.
